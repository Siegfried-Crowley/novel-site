# 📖 公益小说在线阅读站 — 需求与开发方案

> **定位**：公益免费、无需登录、公开浏览的轻量小说阅读站。
> **形态**：独立小项目，与现有 Discord 项目完全分离，单独一个文件夹。
> **目标**：管理端「懒人一键上传 TXT」→ 网站自动解析成小说 → 前台像真小说站一样有封面、简介、分类、排行、阅读页。

---

## 一、技术选型（推荐，理由写清）

| 层 | 选型 | 理由 |
|----|------|------|
| 后端 | **Node.js + Express** | 单进程、零配置、`npm install && npm start` 就跑起来，任何免费主机都能跑 |
| 数据库 | **SQLite**（better-sqlite3） | 一个文件即数据库，不用装数据库服务；备份=复制文件；低并发阅读站完全够用 |
| 前端 | **原生 HTML/CSS/JS**（Express 托管静态页） | 无构建步骤、无框架依赖，打开即用，最省资源最好部署 |
| TXT 编码识别 | **iconv-lite + jschardet** | 中文 TXT 常见 UTF-8 / GBK / GB18030，必须能自动识别转换，否则乱码 |
| 封面 | **服务端自动生成 SVG 渐变封面**（零依赖） | 没传图也能给每本书配一张好看封面；有图则用上传的图 |

> **备选**：如果你更熟 Python，可用 **FastAPI + SQLite**，方案思路完全一样。如果坚持 Java，用 Spring Boot 也行但偏重。**推荐 Node 方案**。

---

## 二、功能需求清单

### 2.1 一键上传（管理端核心）
- 上传 `.txt` 文件（支持多本批量拖拽上传）
- **自动识别**书名（文件名或首行）、作者（常见 `作者：xxx` 行）
- **自动切分章节**：按正则识别 `第X章 / Chapter X / 序章 / 楔子 / 引子 / 前言 / 尾声 / 后记 / 番外 / 第X卷` 等标题行切段
- 自动统计**总字数**（中文字符数）
- 没有封面上传时 → 自动生成 SVG 渐变封面（书名居中显示）
- 上传同名小说 → **覆盖更新**：删除旧数据、写入新章节（弹窗确认）
- 上传后立即跳转到「编辑页」，可预览切分结果

### 2.2 手动修改（管理端）
- 编辑**书籍信息**：书名、作者、分类、简介、封面（重新传图/删图恢复默认）、状态（连载/完结）
- 编辑**章节**：改标题、改正文（textarea）、删除章节、新增章节、调整顺序
- **重新切分**：对已上传的源文件用不同正则重新切分（改错的时候用）

### 2.3 前台展示（真小说站质感）
- **首页**：
  - 顶部轮播推荐位（大图 + 书名 + 简介）
  - 分类导航栏（玄幻/武侠/都市/言情/同人/科幻/历史/游戏…）
  - 「最新更新」区块（最近有章节更新的书）
  - 「热门推荐」区块（按阅读量）
  - 书籍卡片：**封面图 + 书名 + 作者 + 简介摘要 + 状态角标（连载/完结）**
- **列表页**：按分类浏览 / 关键词搜索（书名、作者）/ 排序（最新更新、阅读量、收藏数、字数）
- **详情页**：大封面、书名、作者、分类、状态、简介全文、总字数、章节目录（分页/折叠）、阅读量、收藏按钮、最近更新章节
- **阅读页**：
  - 章节正文（中文排版、行距/字距舒适）
  - 上一章 / 下一章、章节目录抽屉
  - 字号调节（小/中/大）、白天 / 夜间模式
  - **进度记忆**：读到第几章自动记住（localStorage，无需登录），下次续读
  - 每进一章阅读量 +1（后端计数）
- **排行榜页**：三个 Tab —— **阅读榜**（views）、**收藏榜**（favorites）、**更新榜**（最新更新时间）

### 2.4 收藏
- 无需登录：读者点「收藏」用 localStorage 记在自己浏览器
- **全局收藏数**：为去重，首次收藏时后端给一个匿名 cookie，同一浏览器只算一次，展示"共 X 人收藏"

### 2.5 管理端入口与安全
- 后台藏在独立路径（如 `/admin`），正常浏览首页的访客看不到入口
- **建议加一道访问口令**（管理端密码，写入环境变量/配置文件），不是用户注册体系，只是拦住路人上传。**可留空 = 完全开放**（仅限你自己用）。
- 所有上传/编辑接口同样校验该口令

---

## 三、数据设计（SQLite）

### `novels` 书籍表
| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 自增 |
| title | TEXT | 书名 |
| author | TEXT | 作者 |
| category | TEXT | 分类 |
| description | TEXT | 简介 |
| cover | TEXT | 封面 URL（默认 `/covers/auto-<id>.svg`） |
| status | TEXT | `serial` 连载 / `finished` 完结 |
| views | INTEGER | 阅读量 |
| favorites | INTEGER | 收藏数 |
| word_count | INTEGER | 总字数 |
| source_file | TEXT | 原始 TXT 保存路径（供重新切分） |
| created_at / updated_at | TEXT | 创建 / 最新章节更新时间 |

### `chapters` 章节表
| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 自增 |
| novel_id | INTEGER FK | 所属书籍 |
| title | TEXT | 章节标题 |
| content | TEXT | 章节正文 |
| order_index | INTEGER | 章节顺序 |
| created_at | TEXT | 创建时间 |

索引：`chapters(novel_id, order_index)`、`novels(title)`、`novels(views)`、`novels(updated_at)`。

---

## 四、TXT 解析规则（关键实现）

1. **读编码**：jschardet 探测字节流 → iconv-lite 解码（UTF-8/GBK/GB18030 兜底）。
2. **清洗**：去掉 BOM、按行拆分、去掉 `\r`、跳过全空行与重复的分隔符行（`--------` 等）。
3. **取书名/作者**：
   - 书名优先取首行，非标题行则用文件名（去扩展名）。
   - 作者取 `作者[:：]?\s*(.*)` 首处匹配。
4. **切分章节**（核心正则，含首行不匹配时的兜底）：
   ```
   /^\s*(第\s*[0-9一二三四五六七八九十百千万零〇两]+\s*[章回节卷集部篇][^\n]{0,30}|Chapter\s*\d+[^\n]{0,30}|序章|楔子|引子|前言|尾声|后记|终章|大结局|番外[^\n]{0,20}|第\s*[0-9一二三四五六七八九十百千万零〇两]+\s*卷)/i
   ```
   - 若整本匹配不到章节标题 → 整本作为「第 1 章」存下（保证一定能读）。
5. **字数**：统计去除空白后字符数。
6. **切分结果预览**：管理端展示每章标题+首行，允许在编辑页手动改。

---

## 五、页面与路由清单

| 路由 | 页面 | 说明 |
|------|------|------|
| `/` | 首页 | 轮播、分类、最新、推荐 |
| `/list?cat=&q=&sort=` | 列表页 | 分类/搜索/排序 |
| `/book/:id` | 详情页 | 书籍信息 + 章节目录 |
| `/read/:bookId/:chapterId` | 阅读页 | 正文、字号、夜间、目录抽屉、进度 |
| `/rank` | 排行榜 | 阅读/收藏/更新 三榜 |
| `/admin` | 管理首页 | 书籍列表、删除、编辑入口 |
| `/admin/upload` | 上传页 | 拖拽上传 TXT、预览切分 |
| `/admin/edit/:bookId` | 编辑页 | 改书信息、改章节、重新切分 |

### 后端 API
| 方法 | 路径 | 说明 | 鉴权 |
|------|------|------|------|
| GET | `/api/books?cat=&q=&sort=&page=` | 列表（首页/列表页共用） | 无 |
| GET | `/api/books/:id` | 详情（含章节目录） | 无 |
| GET | `/api/books/:id/chapters` | 章节列表 | 无 |
| GET | `/api/chapters/:id` | 章节正文（阅读量+1） | 无 |
| POST | `/api/books/:id/read` | 阅读量计数（防刷可加时间窗） | 无 |
| POST | `/api/books/:id/favorite` | 收藏（发匿名 cookie） | 无 |
| GET | `/api/rank?type=views\|favorites\|updated` | 排行榜 | 无 |
| POST | `/api/admin/upload` | 上传 TXT（multipart） | 口令 |
| POST | `/api/admin/books` | 新增/编辑书籍信息 | 口令 |
| PUT | `/api/admin/chapters/:id` | 编辑章节 | 口令 |
| DELETE | `/api/admin/chapters/:id` | 删除章节 | 口令 |
| POST | `/api/admin/books/:id/reparse` | 重新切分 | 口令 |
| DELETE | `/api/admin/books/:id` | 删除整本书 | 口令 |
| POST | `/api/admin/login` | 校验管理口令（返回会话 token） | — |

---

## 六、项目目录结构

```
novel-site/
├── package.json          # 依赖：express、better-sqlite3、multer、iconv-lite、jschardet
├── server.js             # 入口：Express + 静态托管 + API 挂载
├── db.js                 # SQLite 建表与初始化
├── lib/
│   ├── txt-parser.js     # 编码识别 + 章节切分 + 提取书名作者
│   └── cover.js          # SVG 渐变封面自动生成
├── routes/
│   ├── public.js         # 前台只读 API
│   └── admin.js          # 管理端 API（口令校验中间件）
├── data/                 # 数据库文件 + 上传源文件（gitignore）
│   ├── novels.db
│   └── uploads/
├── public/               # 前端静态页（无构建）
│   ├── index.html  list.html  book.html  read.html  rank.html
│   ├── admin.html  admin-upload.html  admin-edit.html
│   ├── css/style.css     # 整站样式（含夜间模式变量）
│   ├── css/reader.css    # 阅读页排版
│   └── js/（每页一个，原生 fetch）
└── README.md             # 启动说明
```

---

## 七、开发步骤（分阶段推进）

- **M1 骨架**：项目初始化、Express + SQLite 建表、静态页能打开、夜间模式样式变量
- **M2 上传解析**：txt-parser 完成、管理口令、上传页拖拽上传、SVG 封面生成、覆盖更新
- **M3 前台首页**：书籍卡片 + 轮播 + 分类导航 + 最新/推荐区块
- **M4 列表/搜索**：分类浏览、关键词搜索、多维度排序、分页
- **M5 详情/阅读**：详情页目录、阅读页（字号/夜间/目录抽屉/进度记忆）、阅读量计数
- **M6 排行/收藏**：三榜 + 收藏按钮 + 匿名 cookie 去重
- **M7 编辑管理**：改书信息、改/删/增章节、重新切分
- **M8 打磨**：空状态、错误提示、移动端适配、中文排版细节

---

## 八、部署方式

- **本机**：`npm install && npm start`，浏览器开 `http://localhost:3000`（端口可改）
- **公益部署**（免费）：可放任何支持 Node 的免费/低配主机：
  - 国内：轻量云服务器、NAS、旧电脑（挂个花生壳/内网穿透）
  - 国外：Render / Railway / Fly.io 免费档（注意免费档的存储持久性，SQLite 建议挂持久卷）
  - **数据备份 = 复制 `data/novels.db` + `data/uploads/` 两个文件**
- 维护方便：无账号体系，备份即拷库；管理员就一份口令，忘了改配置文件即可

---

## 九、注意事项 / 坑

1. **编码**：GBK 编码的 TXT 不处理会全乱码——jschardet 识别有误时给「强制转码」兜底按钮。
2. **切分正则**：不是每本 TXT 都规范，管理端必须保留「重新切分」能力作为补救。
3. **阅读量防刷**：同一浏览器 30 秒内重复进同一章只 +1（cookie 或内存时间窗）。
4. **封面版权**：自动生成的 SVG 封面确保无版权问题；手动上传封面请用授权图。
5. **内容合规**：公益免费也要注意小说版权——只上传自己写的或已授权/公版作品。
6. **并发**：better-sqlite3 单连接足够；如将来流量大再换 PostgreSQL，字段结构兼容。
